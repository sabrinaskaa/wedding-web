const styles = theme => ({
  root: {
    width: '100%',
    maxWidth: 360,
    backgroundColor: theme.palette.background.paper
  },
  avatar: {
    margin: 10
  },
  bigAvatar: {
    marginTop: 50,
    width: 60,
    height: 60
  }
});

export default styles;
