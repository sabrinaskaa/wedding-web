const styles = theme => ({
  card: {
    display: 'flex',
    marginTop: 10,
    padding: 3
  },
  cover: {
    width: 50
  },
  controls: {
    display: 'flex',
    alignItems: 'center',
    paddingLeft: theme.spacing(1),
    paddingBottom: theme.spacing(1)
  },
  playIcon: {
    height: 38,
    width: 38
  }
});

export default styles;
